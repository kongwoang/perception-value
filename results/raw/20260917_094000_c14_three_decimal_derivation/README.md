Task 17 item 2. Two regenerations of stage C14 on the reference board with BLAS and OpenMP threads pinned to 1 and
to 4 (release 44f940f, cached-tier env), and the three-decimal derivation over every run available: `derive.py`
applies the rule registered in RESEARCH_LOG.md before these runs reported; `precision.py` applies the same rule at
two and one decimals; `table.md` is the table in docs/iclr_causal_threshold.md.
`causal_threshold_verify_run.csv` is a sixth run on this board, from the Task 17 full cached verify; it was folded in
after the first derivation and changed no verdict.
